<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GoogleMapsProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GoogleMapsProject" parent="Base.Theme.GoogleMapsProject" />

    <style name="CustomRatingBarStyle">
        <item name="android:ratingBarStyle">?android:attr/ratingBarStyleSmall</item>
    </style>
</resources>